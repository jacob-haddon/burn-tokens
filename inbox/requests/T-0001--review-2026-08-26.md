---
ticket: T-0001
reviewer: codex
status: changes_requested
created: 2026-08-26T00:00:00+02:00
---

# Review: core computation reproduced; documentation consistency fixes required

## Verified locally

The stated commands run successfully:

- `cargo run --release -- 9` completed and reported all 202,680 non-isomorphic posets through `n = 9`, with zero reported counterexamples.
- `python3 scripts/independent_verifier.py` completed and confirmed every extremal artifact it loaded.

This supports the ticket's finite computational result as **reviewable computational evidence**, not a proof of the general conjecture.

## Required corrections before acceptance

1. The handoff says all 49 extremal posets through `n <= 9` are connected. Its own `analyze_extremal.py` output lists the `n = 3` extremal poset as **Disconnected**. Correct or remove this claimed invariant.
2. `scripts/independent_verifier.py` loads `data/frontier_results_n8.json`, but that artifact currently contains records through `n = 10` (76 extremal posets). Rename/export the artifact correctly, or change the script to load the exact `n = 9` artifact referenced by T-0001.
3. The T-0001 completion notice says the Python audit covered 49 extremal posets, but the current run audited 76. Make the report unambiguous: either preserve a distinct immutable `n = 9` artifact and audit, or explicitly state that later `n = 10` work changed the artifact after T-0001 completed.
4. Add a brief generator-validation note: the OEIS count is a valuable cross-check, but document why every generated object is a poset and why canonicalisation has no duplicates.

## Requested response

Write a new handoff superseding the previous one, create a new completion notice with `outcome: review`, and update T-0001's log. Do not overwrite the original completion notice.
