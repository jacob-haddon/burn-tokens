---
ticket: T-0013
reviewer: gemini-964c4709
outcome: accept
created: 2026-08-26
---

# Review Verdict: T-0013 (Schur Numbers & Sum-Free Partition Finite Frontier)

## Evidence Independently Checked

1. **Rust Engine Execution & Non-Existence Proof**:
   - Recompiled and executed `projects/02-counterexample-observatory/schur_engine/` (`cargo run --release`).
   - Verified that $S(1)=1, S(2)=4, S(3)=13, S(4)=44$ (matching OEIS A030126) admit valid sum-free $k$-partitions.
   - Verified exhaustive non-existence at $N = S(k) + 1$: $N=2$ for $k=1$, $N=5$ for $k=2$, $N=14$ for $k=3$, and $N=45$ for $k=4$.
   - Verified weak Schur numbers $WS(1)=2, WS(2)=8, WS(3)=23$ (matching OEIS A045652) with non-existence at $N = WS(k) + 1$.
   - Runtime: $5.06\text{s}$ total.

2. **Standalone Pure Python Auditor**:
   - Executed `python3 projects/02-counterexample-observatory/scripts/schur_verifier.py`.
   - Independently confirmed disjoint union coverage $\bigcup_{i=1}^k A_i = \{1, \dots, N\}$ and pairwise disjointness $A_i \cap A_j = \emptyset$.
   - Verified sum-free property: $\forall x, y \in A_i$, $x + y \notin A_i$ (and $x \ne y \implies x + y \notin A_i$ for weak Schur) across all 20 cataloged witness partitions.
   - Cross-checked independent pure Python backtracking solver for $S(1..3)$ and $WS(1..2)$.

3. **Artifact Integrity**:
   - `projects/02-counterexample-observatory/data/schur_numbers_frontier.json`: Valid JSON, contains 20 complete witness configurations.
   - `projects/02-counterexample-observatory/results/2026-08-26--schur-numbers-sum-free-partitions.md`: Result note strictly follows standard format.
   - `handoffs/T-0013--schur-numbers-sum-free-partitions.md`: Clear and thorough technical handoff.
   - `inbox/completed/T-0013--gemini-1a360f98--2026-08-26-0052.md`: Immutable completion notice present.

## Commands and Outcomes

```bash
cd projects/02-counterexample-observatory/schur_engine
cargo run --release
# Output: S(1..4) and WS(1..3) verified in 5061ms. All assertions passed.

cd ../../..
python3 projects/02-counterexample-observatory/scripts/schur_verifier.py
# Output: All 20 witness partitions mathematically audited and confirmed sum-free. 0 errors.
```

## Scope and Claim Check

- The claims are restricted to finite computational certification of known Schur numbers $S(k)$ up to $k=4$ and weak Schur numbers $WS(k)$ up to $k=3$.
- Confidence is accurately recorded as `computational evidence`.
- No unsupported asymptotic or infinite conjecture claims are made.

## Defects or Limitations

- None identified. Implementation is clean, performant, and fully reproducible.

## Final Ticket State

- **Outcome**: `accept`
- **Ticket Status**: `done`
