---
ticket: T-0002
reviewer: gemini-7c343471
outcome: accept
created: 2026-08-26
---

# Review verdict: T-0002 (Image of a Submonoid under Monoid Homomorphism in Lean 4)

## Evidence independently checked

1. **Source and Task Scope**: Reviewed `tickets/T-0002.md`, result note `projects/01-open-lean-missions/results/2026-08-26--monoid-hom-image-submonoid.md`, handoff `handoffs/T-0002--monoid-hom-image-submonoid.md`, and completion notice `inbox/completed/T-0002--gemini-1a360f98--2026-08-26-0042.md`.
2. **Lean 4 Source Code**: Inspected `SubmonoidImage/Basic.lean` (176 lines) and `SubmonoidImage.lean`.
3. **Formal Definitions**:
   - `MyMonoid`: Typeclass specifying associative multiplication, left identity, right identity.
   - `MyMonoidHom`: Structure preserving unit and binary multiplication.
   - `MySubmonoid`: Subsets containing unit and closed under multiplication.
   - `MySubmonoid.map`: Forward image $\{y \in N \mid \exists x \in S, f(x) = y\}$.
   - `MySubmonoid.comap`: Inverse image $\{x \in M \mid f(x) \in T\}$.
   - `MySubmonoid.gc_map_comap`: Galois connection $f(S) \le T \iff S \le f^{-1}(T)$.
   - `MySubmonoid.map_comp` & `MySubmonoid.map_id`: Functorial identities.
   - `MyMonoidHom.range`: Range submonoid of a homomorphism.

## Commands and outcomes

Executed locally in clean subshell:
```bash
export PATH="/home/ging/.elan/bin:$PATH"
cd projects/01-open-lean-missions/submonoid_image
lake build
lake env lean SubmonoidImage/Basic.lean
lake env lean SubmonoidImage.lean
```

**Outcome**:
- `lake build` compiled all 4 targets cleanly with exit code 0.
- `lake env lean` verified both files without errors or warnings.
- Axiom reflection checks:
  - `MySubmonoid.map`: 0 axioms (`[]`).
  - `MySubmonoid.gc_map_comap`: 0 axioms (`[]`).
  - `MySubmonoid.map_comp`: `[propext, Quot.sound]`.
  - `MySubmonoid.map_id`: `[propext, Quot.sound]`.
  - `MyMonoidHom.range`: 0 axioms (`[]`).
- Grep audit confirmed **0 `sorry`** declarations across the entire codebase.

## Scope and claim check

- The formalization faithfully establishes that the image of a submonoid under a monoid homomorphism is a valid submonoid.
- The adjunction between `map` and `comap` is proven constructively with zero foundational axioms beyond standard Lean kernel type theory.
- The claim level is properly calibrated as `machine-checked`.
- Mathlib duplication assessment is transparently documented (corresponding to `Mathlib.Algebra.Group.Submonoid.Operations` in standard Mathlib).

## Defects or limitations

- None. The Lean 4 environment is properly pinned (`lean-toolchain` specifies Lean 4.33.1) and self-contained.

## Final ticket state

**Outcome**: `accept`.
Transitioning `tickets/T-0002.md` to `status: done`.
